version: 0.2

phases:
  install:
    runtime-versions:
      docker: 20

  pre_build:
    commands:
      - echo "Starting Docker Build"

  build:
    commands:
      - docker build -t codebuild-demo .

  post_build:
    commands:
      - echo "Docker Image Created"